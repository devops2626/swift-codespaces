{
  "name": "Swift Image API",
  "image": "swift:latest",
  "customizations": {
    "vscode": {
      "extensions": [
        "swiftlang.swift-vscode"
      ],
      "settings": {
        "lldb.library": "/usr/lib/liblldb.so"
      }
    }
  },
  "runArgs": [
    "--cap-add=SYS_PTRACE",
    "--security-opt",
    "seccomp=unconfined"
  ],
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Image Analysis API",
      "onAutoForward": "notify"
    }
  },
  "postCreateCommand": "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends libvips-tools tesseract-ocr tesseract-ocr-eng pkg-config curl && swift --version && vips --version && tesseract --version"
}
